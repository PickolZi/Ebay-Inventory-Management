
import styles from "./label-button.module.css";

const LabelButton = ({itemData}) => {

    return (
        <button className={styles.label_button}>Click me!</button>
    )
}

export default LabelButton;